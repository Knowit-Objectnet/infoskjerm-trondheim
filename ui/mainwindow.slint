import { Button, VerticalBox } from "std-widgets.slint";

import { GridBox , HorizontalBox} from "std-widgets.slint";
import { Xkcd, XkcdWidget } from "xkcdwidget.slint";
import { WeatherWidget, WeatherAdapter, Forecast } from "weatherwidget.slint";

export { WeatherAdapter, Forecast }

export component MainWindow inherits Window {
    in property <string> time: "12:34:56";
    in property <string> date: "01";
    in property <string> month: "jan" ;
    
    

    //TODO: refactor into custom struct
    in property <Xkcd> xkcd: {
        title: "Lorem Ipsum",
        image: @image-url("../img/xkcd.png"),
        flavor_text: "Dolor sit amet"
    };

    title: "Infoskjerm";
    //todo: set these programtically
    // width: 540px;
    // height: 960px;
    background:  #030201;
    default-font-family: "sans";


    
    VerticalLayout{
        alignment: space-between;

        WeatherWidget {}
        
        XkcdWidget {
            max-height: 30rem;
            xkcd: xkcd;
            }

        HorizontalBox {
            alignment: space-between;
            VerticalLayout {
                alignment: end;
                spacing: -3px;
                padding-bottom: 6px;
                Text {
                    font-size: 2.4rem;
                    font-weight: 700;
                    horizontal-alignment: center;
                    color: whitesmoke;
                    text: date;
                }
                Text {
                    font-size: 1.1rem;
                    horizontal-alignment: center;
                    color: whitesmoke;
                    text: month;
                }
            }   
    
            Text {
                color: whitesmoke;
                horizontal-alignment: right;
                font-size: 4rem;
                text: time;
            }
    }
        
    }
}